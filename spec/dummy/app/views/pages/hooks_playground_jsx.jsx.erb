import HooksPlaygroundJsx from '<%= Rails.root.join("app", "views", "components", "HooksPlaygroundJsx.jsx") %>';

const HooksPlaygroundJsxPage = ({ page_title, initial_count, initial_label }) => {
  return (
    <main>
      <div className="card">
        <h1>{page_title}</h1>
        <p>This JSX page reuses the shared hook playground component.</p>
      </div>
      <div className="card">
        <HooksPlaygroundJsx initialCount={initial_count} initialLabel={initial_label} testId="jsx-full-page" />
      </div>
    </main>
  );
};

export default HooksPlaygroundJsxPage;

